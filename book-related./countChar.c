// count characters in input, V-1
# include <stdio.h>

int main()
{
    long nc;

    nc = 0;
    while (getchar() != EOF)
        ++nc;
        printf("%ld\n", nc); // ld = long int

    // for (nc = 0; getchar() != EOF; ++nc)
    //     ;
    // printf("\nhola bros\n"),
    // printf("%.0f\n", nc);
        
    
}